services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: server
    ports:
      - "3000:3000"

  client:
    build:
      context: .
      dockerfile: Dockerfile
      target: client
    ports:
      - "5173:80"   # Frontend unter http://localhost:5173
